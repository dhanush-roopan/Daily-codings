#include <stdio.h>

int main(){
  int n;
  scanf("%d",&n);
  int marks[n];
  for(int i = 0;i<n;i++){
    printf("Enter Mark %d : ",i+1);
    scanf("%d",&marks[i]);
    int sum +=marks[i];
    }
    double avg = (double) sum/n;
    printf("The average is : %.2f",avg);
}
